{
  "name": "lightning-exchange-arbitrage",
  "version": "1.0.0",
  "description": "Uniswap V2 Flash Swap Arbitrage on testnet",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "arbitrage": "hardhat run scripts/arbitrage.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^17.4.2"
  }
}
